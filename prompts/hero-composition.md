ASSIGNED FRONT-PAGE HERO RECIPE: **{{recipe}}**

This assignment is code-owned and authoritative. Expand only this one recipe
into `hero_blueprint`; do not rename it, blend it with another topology, or
repeat its structural details in the general design-direction narrative. The
blueprint is front-page-only context.

The complete reviewed default is:

```json
{{blueprint_defaults}}
```

Preserve those defaults unless the selected recipe fragment explicitly permits
another bounded value and the site-specific creative direction benefits from
it.

SELECTED RECIPE FRAGMENT (the only recipe visible in this request):

{{composition_recipe}}
