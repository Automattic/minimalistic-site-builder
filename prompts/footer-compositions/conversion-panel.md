**conversion-panel** — Build a bold, offset invitation with one short heading
and at most ONE wp:button pointing to a purposeful destination allowed by the
FOOTER NAVIGATION/ACTION RULE. Pair it with a small identity/credit block,
not a sitemap. When the final section already owns the primary conversion,
keep the offset panel but make it a non-repeating brand coda and omit the
button rather than duplicating the CTA.
