**contact-sheet** — A film contact sheet closes the page: EXACTLY THREE
square content images in one equal-width row, each depicting a DIFFERENT
real subject from the site's subject matter — three distinct subjects,
three distinct compositions, never one photo three times. A compact
identity lockup sits above the row and ONE utility baseline with the credit
sits below it. Every image's AI_IMAGE aspect-ratio is `square`, never `portrait` or a
card ratio; no captions, no overlays, and never more or fewer than three
frames.
