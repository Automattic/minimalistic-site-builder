**sunken-wordmark** — Invert the billboard: every utility layer — the link
rail, at most one verified contact fact, and the credit line — sits in ONE
compact horizontal band at the TOP of the footer, and the shared FIT-TEXT
IDENTITY LINE closes the page as the LAST element, built at full contrast.
Zero bottom padding on the wordmark block and on the root group so the
letters land flush on the page's bottom edge; nothing — no rule, no credit,
no spacer — renders below the wordmark.
