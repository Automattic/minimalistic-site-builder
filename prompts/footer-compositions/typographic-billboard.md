**typographic-billboard** — One display-scale identity or short brand-coda
heading dominates the band. Use wp:site-title only when the supplied THEME
TOKENS style it at display scale; otherwise use a real heading containing the
spec's exact identity so global utility-sized site-title styles cannot shrink
the focal gesture. Put all useful navigation/contact/copyright in ONE compact
horizontal baseline or wrapped flex rail beneath it. No link columns and no
image.
