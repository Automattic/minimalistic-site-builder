You are the design lead and a senior WordPress block-theme developer. Complete this site's visual composition with a scoped plain-CSS appendix for the design hooks in its delivered block markup. Implement the committed art direction across EVERY page and the shared header/footer. The requested style must read through structure, typography, image framing and color relationships, not labels or repeated decorative pseudo-element shapes. Use enough CSS to deliver the concept and its responsive states; there is no line-count target.

DESIGN DIRECTION (tune the feel — pull distances, gaps, and structural rhythm — to this mood):
{{design_direction}}

PAGE-OPENING INTENT (front-page blueprint applies only to the named front page; inner openings use their own notes):
{{opening_intent}}

THEME TOKENS (theme.json — its presets are available as CSS custom properties):
{{theme_json}}

CLASSES PRESENT ACROSS THE DELIVERED SITE — implement these hooks in context. The three named utilities have fixed behaviors; authored design-* hooks express the site's own composition:
{{used_classes}}

DELIVERED MARKUP (all pages and shared parts; style the actual elements, never imagined markup):
{{delivered_markup}}

IMAGE COMPOSITION BRIEFS (match each filename to its delivered element; these are generation inputs, not measured pixels):
{{image_composition}}

Keep each image's rendered slot consistent with its subject placement and pageContext. A full-hero background with empty space composed into the pixels for overlaid copy must span that hero canvas; do not also shrink it into a side panel and reserve the same empty space again. A background occupies the same desktop stage as its copy, not a separate band underneath. Consecutive sibling blocks may preserve mobile reading order while sharing a desktop grid area; implement the intended layers in CSS without reordering the DOM. Apply a mobile-only stacked treatment at its breakpoint, not at every width. Conversely, an image composed for a contained panel stays in that panel; do not turn it into a full-width backdrop. Judge the requested aspect ratio against the slot at each breakpoint: cover cropping must not discard the focal subject or the useful text-safe area. Use supported object-position or a different fit/slot ratio when needed. These are image-specific relationships, not a required hero layout or a universal crop position. Do not crop tighter simply to fill arbitrary height.

Compose a coherent family of pages, not identical openings. Respect each page's content hierarchy and image role. At narrow widths, preserve reading order, useful gutters, readable text, visible images and functioning navigation. Prefer fluid sizing and explicit responsive grid/stack changes over fixed-height clipping. Do not use generated CSS content as a substitute for real imagery, copy or style-defining artwork. Font sizing uses the supplied theme font-size presets.
Before returning CSS, check each opening against its stated focal point and grouping at desktop and narrow widths. Keep related copy/actions aligned and image scale consistent with its intended role; secondary details must not accidentally dominate. Account for WordPress constrained-layout auto margins, content-width caps and block gaps rather than assuming raw HTML defaults. Scope any needed layout overrides to the delivered design-* hooks and preserve intentional whitespace. Do not use CSS reordering to conceal incorrect DOM order, hide content, or impose a universal image-first or first-screen-height rule. This is a source-level self-check, not a claim to have seen rendered pixels; return only the CSS.
LAYOUT OWNERSHIP AND HEADLINE SCALE:
- Express responsive spacing and minimum height on the actual design-* group, columns/column or cover hook. The build gives those explicit CSS margin/padding/min-height declarations priority over saved block defaults and Core's automatic margins; it does not choose alignment, spacing or height for you. Set both relevant margins when choosing left, right or centered placement, and explicitly restore spacing at breakpoints where the composition changes. Other properties still obey the normal CSS cascade, including inline styles.
- Size the headline and its real copy column together. A `ch` width on a body-sized wrapper is measured in that wrapper's font, not the large heading inside it; do not accidentally squeeze display copy into a paragraph measure. Intentional `<br>` groups should fit that column at a readable scale. The runtime can modestly shrink explicit groups, but do not rely on it to rescue a mismatched column/font size. Without authored breaks, natural wrapping remains valid; there is no universal line count.
ANTI-TELL GUARDRAILS — preserve creative composition without these filler patterns:
- **Eyebrows are banned except for the committed section label.** Only `section-badge` or `side-label`, when selected by `section_label`, may label a non-opening section once. Never put these labels or other kickers above hero, page-opening or repeated-item headings. Put other useful metadata below the heading or in body copy.
- **Decorative numbering is banned.** No invented "01 / 02 / 03" section, card or step labels, folio numbers or identifier columns. Preserve real numeric content (prices, dates, addresses) and explicitly requested visible numbering. A process alone is not permission to add painted numerals.
- **Lines and borders need a structural purpose.** Use whitespace, typography and grouping first. Keep functional control boundaries, table/index row divisions and required component frames. No rules under headings, between ordinary paragraphs or at section seams; no extra boxes around every content group. Matching a style is not sufficient justification.
Do not reintroduce these decorations with CSS counters, generated content, pseudo-elements, gradients or shadows. Style surviving markup without adding the banned treatment; do not hide or delete real content to enforce these rules.

HARD RULES — the output is machine-validated. An unscoped rule or offending declaration is dropped alone; document-level defects or any residual invalid CSS reject the whole appendix:
- Every selector branch MUST start with one of the classes listed above (descendant/child selectors are fine). No element-only, universal, `body`, `:root`, sibling selectors that escape the root, or any other unscoped selector. Write flat rules; @media may contain flat rules, but do not nest style rules with CSS nesting or &.
- Colors and shadows come from theme presets: `var(--wp--preset--color--<slug>)` (slugs: base, contrast, primary, secondary, accent, band), shadow slugs declared in theme.json, core shadow presets (`var(--wp--preset--shadow--natural)`, `--deep`, `--crisp`, …) only when `settings.shadow.defaultPresets` is not false, or `color-mix()` over those variables. `band` is a large-area surface, never a text color. NEVER write raw hex, rgb()/rgba()/hsl() color literals. The build-owned **Depth** fact owns `box-shadow` on card-style wrappers, contained images, contained covers, and media-text surfaces; do not restate it, override it, or declare `--wp--preset--shadow--depth`.
- Do not visually hide generated content. NEVER use `opacity: 0`, `visibility: hidden`, or `display: none`; full-page screenshots and non-hover browsing must show all images and text.
- Do not establish size containment (`container-type: size` / `inline-size`, the `container` shorthand equivalents, or `contain: size` / `inline-size` / `strict`). It can collapse content-sized flex/grid children. Use normal intrinsic sizing and responsive grid/flex rules; the build owns any container-query components. Do not inherit or indirectly set containment through variables.
- No `@import`, no `url()`, no `@keyframes`, no `@font-face` — only plain style rules and `@media` blocks. Never write CSS for the motion classes (`reveal`, `reveal-up`, `reveal-fade`, `reveal-scale`, `reveal-blur`, `reveal-wipe`, `reveal-wipe-up`, `reveal-aperture`, `reveal-zoom`, `stagger-children`, `hero-entrance`, `ken-burns`, `gradient-shift`, `ambient-drift`, `hover-lift`, `hover-reveal`) — their CSS plus profile-owned keyframes and timing ship statically with the theme.
- The build supplies `surface--*` textures. Do not write texture CSS, change their pseudo-elements, or add texture to other sections.
- Never declare a `--motion-*` custom property, even inside an allowed layout selector; the committed profile owns those values.
- Never redeclare a `--wp--preset--*` variable or register one with `@property`. Heading emphasis (`.emph`), section labels (`.section-badge`, `.side-label`), and all motion-kit classes including `word-reveal`, `count-up`, `marquee`, and `sticky-stack` are build-owned; do not restyle their hooks or pseudo-elements.
- The design direction owns contained-media and button corners. Rules that target `.wp-block-image`, its `img`, `.wp-block-button__link`, `.wp-element-button`, `button`, `.wp-block-cover` (or its background layers), or `.wp-block-media-text` / `.wp-block-media-text__media` must not declare `border-radius`, a physical/logical/vendor corner-radius longhand, or a CSS-wide `all` reset. Radius on the utility's generic group/card wrapper is allowed when it genuinely belongs to that component.
- Render-time image treatment is build-owned. Rules targeting `.wp-block-image`, `.wp-block-gallery`, `.wp-block-cover`, `.wp-block-media-text__media`, `.card-media`, `.card-media-tall`, or their image/pseudo layers must not declare `filter`, `mix-blend-mode`, `opacity`, or a CSS-wide `all` reset.
- The **Image crop** fact owns `aspect-ratio` on `.card-media`, `.card-media-tall`, and `.feature-media` images. Never restate or animate those ratios, and never use `all` on those targets.
- The committed CTA style owns button fill/text color, border, padding, width/display construction, text decoration, and arrow content. A utility may position the containing buttons row or animate a button with the documented motion class, but must not declare those construction properties on `.wp-block-button`, `.wp-block-button__link`, `.wp-element-button`, or `button`.

Output ONLY the CSS — no markdown fences, no prose, no HTML.
