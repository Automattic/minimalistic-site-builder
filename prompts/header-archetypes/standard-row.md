site title (wp:site-title) or logo on one side, primary wp:navigation on the other, in a single top-level wp:group with a row/flex layout and space-between justification. ONLY when the contract's `header.displays_tagline` is true, stack wp:site-tagline directly under the title (`"fontSize":"caption"`, quieter color) inside an inner wp:group with `"style":{"spacing":{"blockGap":"0"}}` so the pair reads as one identity unit on the row's start side; when it is false there is NO tagline text to render — never emit the block. The conventional choice — lean on the direction's typography and spacing so it doesn't read as a default.
