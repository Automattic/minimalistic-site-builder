a logo-led brand lockup on one side, navigation on the other: wp:site-logo (small — a `width` in the 40-64 range with `"shouldSyncIcon":true`, optionally `"className":"is-style-rounded"`) beside wp:site-title (a compact preset size). ONLY when the contract's `header.displays_tagline` is true, stack wp:site-tagline under the title (a step smaller, quieter color) inside an inner wp:group with `"style":{"spacing":{"blockGap":"0"}}` so the pair reads as one unit; when it is false there is NO tagline text to render — the block would paint a blank line, so the lockup is the logo beside the title alone. Suits friendly, product, boutique, and small-business directions.
