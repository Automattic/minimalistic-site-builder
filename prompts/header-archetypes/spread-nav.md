an edge-to-edge bar with two zones: the wordmark at the start and the navigation taking the whole remaining width, its four or five links spread with space between them. Top-level wp:group (`"layout":{"type":"constrained"}`, a `backgroundColor` per the SEAM rule, the same `sm` padding) holding ONE inner wp:group with `"className":"header-spread"`, `"align":"wide"` and `"layout":{"type":"flex","flexWrap":"nowrap","justifyContent":"space-between","verticalAlignment":"center"}`. Inside, in this order: wp:site-title (or wp:site-logo), then ONE wp:navigation with four or five links. NO wp:buttons: the spread bar has no CTA; the trusted kit spreads the items. Never a tagline, never a second row. Suits noir, brutalist and poster traditions whose header is a typographic ruler across the page.
