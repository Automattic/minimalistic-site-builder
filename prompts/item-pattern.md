ASSIGNED ITEM PATTERN (authoritative repeated-item contract): `{{item_pattern}}`

This section is list-like, so repeat its items with this exact idiom. Do not
substitute the site's card default or another familiar list treatment. The
section root MUST also carry `{{root_marker}}`, and every repeated item MUST
carry `item-pattern__item` on its own block wrapper.

{{item_pattern_recipe}}
