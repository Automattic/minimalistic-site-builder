You are writing the design document a developer will follow to build a WordPress block theme. You have two inputs.

SITE SPEC (JSON):
{{site_spec}}

DESIGN DIRECTION (JSON):
{{design_direction}}

Write a clear, well-structured Markdown design document named for this site. Consolidate the spec and direction into concrete build guidance. Include these sections:

# <Site Name> — Design Document

## Overview
Who the site is for, the concept, and the feeling to achieve.

## Color System
The exact palette with hex values and the role of each color (background, text, primary, secondary, accent). Note contrast/readability.

## Typography
The heading and body fonts, when each is used, and the size/hierarchy approach.

## Spacing & Layout
Spacing rhythm, content width, density, and the overall page structure.

## Components
Concrete styling for buttons, links, cards, navigation, and forms.

## Imagery & Iconography
Direction for photos, illustrations, and icons.

## Landing Page
The ordered sections of the home page and what each contains.

## Do / Don't
Bullet lists of concrete guidance.

Be specific and consistent with the inputs. Use real hex values and font names. Output only the Markdown document.
