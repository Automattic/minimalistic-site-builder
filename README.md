# BIGR-848 — navigation parity evidence

Screenshots referenced from Automattic/minimalistic-site-builder#304 and
Automattic/blocks-engine#899. Kept on an orphan branch so they stay out of the
pull request diff.

- `nav-parity/00-summary-grid.png` — the whole comparison in one image
- `nav-parity/design-<slug>.png` — the source design's header
- `nav-parity/before-<slug>.png` — built on `trunk` with published transformer v0.4.25
- `nav-parity/after-<slug>.png` — built on `nav-parity/review-fixes`

Header crop at 1366px, fonts settled, admin bar removed, each build's identity
confirmed from its `/themes/<slug>/` stylesheet href.

Delete this branch once the pull requests are merged.
