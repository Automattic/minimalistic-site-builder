{
    "$comment": "Default LLM models per provider for each pipeline step. Pick the active provider with `php bin/build.php --provider=<name>` or the LLM_PROVIDER env var (falls back to default_provider below). Every step is tagged with a tier (large | small); the active provider's model for that tier is used. Small = fast/cheap structural steps; large = quality-critical creative/generation steps. OVERRIDES (any model id, always win): LLM_MODEL_<STEP> for one step (e.g. LLM_MODEL_SITE_SPEC=gpt-5.5), LLM_MODEL for the whole run's large tier, LLM_MODEL_SMALL for the small tier.",
    "version": 1,
    "default_provider": "anthropic",
    "providers": {
        "anthropic": {
            "large": "claude-opus-5",
            "small": "claude-haiku-4-5"
        },
        "openai": {
            "large": "gpt-5.5",
            "small": "gpt-5.4-mini"
        },
        "xai": {
            "large": "grok-4.6",
            "small": "grok-4.6"
        },
        "openrouter": {
            "large": "moonshotai/kimi-k3",
            "small": "moonshotai/kimi-k2.5:nitro"
        },
        "baseten": {
            "large": "moonshotai/Kimi-K3",
            "small": "zai-org/GLM-5.2-Fast"
        },
        "hybrid": {
            "$comment": "Mixed-transport set: Baseten serves the run, and the steps under `steps` go elsewhere. `transport` names the client every other step uses; each `steps` entry names its own transport and model. RoutingLlm dispatches per request on the model id, so no step knows more than one provider is in play. gpt-5.6-sol is served by OpenAI directly: the wpcom proxy reaches OpenAI over /v1/responses, which this client does not speak.",
            "large": "moonshotai/Kimi-K3",
            "small": "zai-org/GLM-5.2-Fast",
            "transport": "baseten",
            "steps": {
                "inner-pages-design": {
                    "transport": "openai",
                    "model": "gpt-5.6-sol"
                },
                "sections": {
                    "transport": "openai",
                    "model": "gpt-5.6-sol"
                },
                "page-styles": {
                    "transport": "openai",
                    "model": "gpt-5.6-sol"
                }
            }
        }
    },
    "step_tiers": {
        "refine-prompt": "small",
        "site-spec": "small",
        "design-direction-seeds": "small",
        "design-direction": "large",
        "theme-json": "large",
        "page-plan": "small",
        "sections": "large",
        "page-styles": "large",
        "custom-motion": "large",
        "image-prompt-repair": "small"
    }
}
