{
  "name": "screenshot",
  "private": true,
  "description": "Full-page screenshot capture that scrolls to trigger lazy-loaded images",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "playwright-core": "1.61.1"
  }
}
